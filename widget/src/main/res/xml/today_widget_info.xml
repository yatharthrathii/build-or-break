<?xml version="1.0" encoding="utf-8"?>
<!--
    One size that works rather than five that are mediocre. Four columns by two
    rows is the smallest cell that fits a time, a title on two lines and two
    buttons, and resizing is allowed horizontally so a wider home screen gets a
    longer title rather than a stretched one.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description"
    android:previewLayout="@layout/widget_preview"
    android:initialLayout="@layout/widget_loading"
    android:updatePeriodMillis="1800000"
    tools:targetApi="s" />
