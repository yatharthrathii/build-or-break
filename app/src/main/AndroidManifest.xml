<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Permissions are added in M4 alongside the code that uses them, not
        before. See techspec.md section 7 for the full list and the Play Console
        declaration each one needs:

          USE_EXACT_ALARM, SCHEDULE_EXACT_ALARM, POST_NOTIFICATIONS,
          USE_FULL_SCREEN_INTENT, FOREGROUND_SERVICE,
          FOREGROUND_SERVICE_SPECIAL_USE, RECEIVE_BOOT_COMPLETED, VIBRATE

        Declaring a restricted permission before there is a working fallback for
        it is how a release gets rejected, so they land together.
    -->

    <application
        android:name=".BuildOrBreakApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuildOrBreak.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:theme="@style/Theme.BuildOrBreak.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
